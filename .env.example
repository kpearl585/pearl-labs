# Pearl Labs — Environment Variables
# Copy to .env.local and fill in your keys

# Image generation (at least one required for img:* scripts)
OPENAI_API_KEY=sk-...          # GPT Image — final renders (~$0.04-0.08/image)
FAL_KEY=...                    # fal.ai FLUX — cheap concepting (~$0.01-0.03/image)

# Email (for contact form)
RESEND_API_KEY=re_...
